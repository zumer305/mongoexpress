<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h3>All Chats</h3>
    <form method="GET" action="/chats/new">
        <button>New chat</button>
    </form>
  <%  for(let chat of chats){%>
    <div class="chat">
        <p>From: <i><%=chat.from%></i></p>
        <div class="msj">
              <p><%=chat.msj%></p>
              <form method="GET" action="/chats/<%=chat.id%>/edit"><button>Edit</button></form>
              <br>
               <form method="POST" action="/chats/<%=chat.id%>?_method=DELETE"><button>Delete</button></form>
        </div>
      
        <p>To:<i><%=chat.to%></i></p>
        <hr/>
        <p><%=chat.created.toString().split(" ")[4]%></p>
          <p><%=chat.created.toString().split(" ").slice(0,4).join(" ")%></p>
    </div>
    <br>
    <br>
<%}%>
</body>
</html>
